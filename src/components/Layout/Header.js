import React from 'react';
import classes from '../../assets/sass/layout/_Header.module.scss';

const Header = (props) => { 
  return (
    <section className={classes.header}>
      
    </section>
  )
}

export default Header;